# Sample run — 2026-08-28, fresh scenario

A complete finished game, committed so the payoff is visible without
spending a token. **Spoilers throughout, including in the briefs.**

Open [`report.html`](report.html) in a browser. It was rendered
deterministically from [`events.jsonl`](events.jsonl) by
`../render-report.mjs` — 51 events, 40 messages, all logged live during
play by the game-master session.

This run exercised **fresh-scenario mode**: the Ghost authored a new
solution and witness web (the four briefs in
[`scenario-briefs/`](scenario-briefs/), a useful reference for writing
your own) and summoned with `summon.sh -b`. The board it produced was
the demo's best spread — a winner, an echo, a genuine miss, and the
murderer's frame — plus every pi-post mechanic on the record: a
`queued` delivery to a retired suspect who resumed and fired his one
shot from orders read at waking, dawn arriving from a shell-timer
process, and a whisper hour that undid the killer.
